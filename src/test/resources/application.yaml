spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: admin
    password: 2222

  jpa:
#    generate-ddl: true
    hibernate:
      ddl-auto: none


    show-sql: true
    properties:
      hibernate:
        format_sql: true

  sql:
    init:
      mode: always